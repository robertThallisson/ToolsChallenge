spring:
  application:
    name=toolsjavachallenge
  datasource:
    url: jdbc:postgresql://localhost:5432/toolschallenge?createDatabaseIfNotExist=true&useSSL=false
    username: postgres
    password: mund0b1t1nf0
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        temp:
          use_jdbc_metadata_defaults: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    flyway:
      enabled: true
      locations: classpath:db/migration
      baseline-on-migrate: false
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
